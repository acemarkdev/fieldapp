# ACE Field App — Roadmap / Pipeline

A running backlog. Append as we go.

## Done
- Multi-tenant Supabase schema + RLS (ACE = tenant #1)
- Shared domain logic (code assembly, ranking, fitting rate) with tests
- Sync API → Monday: match-by-title, idempotent create/update
- Supabase → Monday promote loop (canonical store is the source of truth)
- Fitter team → Monday Fitters column; fitting rate → Labour Cost (default + per-item override)
- Snags as duplicate Monday items (Install Status = Snag, no Labour Cost, name + comment)
- Snag / after-install photos → Supabase Storage → Monday file columns (Design Sketch, Picture After)
- Install / completion flow (Install Status, Actual Install Date, after-photo)
- Store report CLI; seed + batch-promote
- Live read-only web dashboard
- **Office web app — Stage 1**: Supabase-Auth login, view jobs/items, edit rate/status/team, Sync-to-Monday
- **Office web app — Stage 2**: teams & rates management (admin-gated CRUD); Monday-sync tab (link a job to its board from the UI, per-job synced/unsynced counts, batch Sync-all)
- **Office web app — Stage 3a**: create new survey items from the desk (assembles the full code, inserts as 'surveyed'); item detail drawer (all fields + photos via signed URLs)
- **Office web app — bulk select**: row checkboxes + select-all header; bulk bar to Sync selected, assign team, or set install status across many items (tenant-guarded)
- **Monday links**: store per-job account slug so item links resolve to the right account; robust board-id parsing from a pasted board URL
- **Office web app — user management**: admin-only Users tab — invite (create app_users row + Supabase login), set role, activate/deactivate, reset password; self-lockout guards
- **Office web app — snags from the desk**: raise a snag from the item detail drawer (comment + optional labour cost + team + photo)
- **Live wallboard** (`/live`): standalone dark, auto-refreshing (30s) status page, key-gated via `LIVE_KEY`, pinnable as a browser tab; shares the dashboard figures. (A Monday-backed Cowork artifact version is still an option for remote/pin-in-Claude viewing.)
- **Microsoft SSO**: optional "Sign in with Microsoft" on the office login (Supabase Azure OAuth, implicit flow, no browser deps), gated by `AZURE_SSO_ENABLED`; server links the identity to `app_users` by email. Setup: `docs/sso-setup.md`.
- **Versioning**: `APP_VERSION` + `CHANGELOG` in `packages/shared/src/version.ts` and `CHANGELOG.md`; office header shows a version chip that opens a "What's new" list. Current: **v0.5.0**.
- **Snags are first-class items** (`survey_items.kind='snag'`, `parent_item_id`, `-S<n>` code): a snag carries its own labour cost + fitter team, appears in the items list with a SNAG badge, and syncs to Monday through the normal promote path (Install Status = Snag, its own Labour Cost/Fitters, photo → Design Sketch). The mobile app will use the same model.

## Next (near-term)
- **SSO login (Microsoft / Entra)** — Stage 2+. Foundation already in place (Supabase Auth). OAuth is free-tier; SAML is paid/enterprise. Microsoft is the natural fit (ACE/Axis are Microsoft 365). ~session-sized: provider config + a "Sign in with Microsoft" button; app_users linking by email is unchanged.
- **Mobile app (Expo)** — *v0.4.0, running on device (SDK 54)*: `apps/mobile` in Expo Go — sign in, jobs list, items list, **item detail** (set install status), **New item** (survey), and **photos** (take/choose on the device → Supabase Storage → thumbnails on the item; pushed to the Monday Design Sketch column on sync, once each). Direct Supabase access with the anon key; tenant-scoped by RLS incl. Storage (migration 0006).
- **Mobile offline queue** — *v0.6.0*: new items **and photos** are queued in local storage and pushed to Supabase when back online (on save, on screen focus, on app resume, or a "Sync now" button). Photos taken during an offline survey resolve to their item automatically once it syncs (matched by full code) and land in Monday's Design Sketch on the office sync. Jobs/items/teams cached for no-signal navigation; pending items/photos and an offline banner show what's waiting.
- **Mobile New-item smart inputs** — *v0.7.0*: tap-first survey form — Block = number → `B1`; Elevation = E1–E4 buttons + manual E5+; Flat/Floor = numeric (Floor → `F1`); Item = W/D toggle + number → `W02`/`D01` with **Type auto-set** (Window/Door). Produces the identical full code + stored values as the office form.
- **Mobile Room picker** — *v0.8.0*: quick chips for the common rooms (Living room/Kitchen/Bathroom/Bedroom/Hallway) + a searchable **More…** modal with all 22 ACE room codes (`apps/mobile/src/lib/rooms.ts`); selecting stores the code (LR, KT, …). Works offline.
- **Mobile Material/Glazing/Glass pickers** — *v0.9.0*: Material chips (uPVC/Aluminium/Timber/Composite/Steel); Glazing chips defaulting to **Triple**; Glass defaulting to **4-20-4** with a searchable list of common specs (`apps/mobile/src/lib/glass.ts`) + free-type custom. The whole survey form is now tap-first. Block/Elevation/Floor are required (guards against sparse codes; relax per-job later if a job has no blocks).
- **Mobile edit/delete pending items** — *v0.10.0*: tap a queued (not-yet-synced) item to reopen it in the form pre-filled, edit and **Save changes** (updates the queued record in place, re-points its queued photos) or **Delete** it before it uploads.
- **Mobile add Job** — *v0.11.0*: **+ New job** on the Jobs screen — client code (pre-filled from existing jobs), job code, name, optional address → inserts into `jobs` via anon+RLS (tenant from app_users/jobs). Monday board still linked in the office. Online-only for now. Next: mirror smart inputs into the office form (optional); offline status changes; config/style picker.
- **Roles & access — foundation** — *v0.12.0*: a capability matrix (`role → capabilities`) is now the single source of truth in `packages/shared/src/permissions.ts` (`can(role, cap)`), documented in `docs/roles-and-access.md`, and shown in a new admin-only **Roles** tab in the office app. Defaults: admin = all; office = everything but user management; surveyor = create/edit items + snags + photos (no jobs); scanner = create items + photos; fitter = fit items + snags + photos, and sees only ready-to-fit items. Enforcement lands next in three layers (UI gating, office API checks, and — the real boundary — role-based RLS policies, since mobile writes to Supabase directly). Plan tracked in the doc's action list (B–F).
- **Role enforcement — database (RLS)** — *v0.13.0*: `supabase/migrations/0007_role_access.sql`. Restrictive write policies (AND-ed with the existing tenant policies) enforce the matrix on the mobile/direct-Supabase path — surveyors add items but not jobs; fitters set install status but not the spec (a trigger whitelists the fitter's editable columns); only admin/office manage jobs, teams, deletes. The office server (service-role key) bypasses RLS and gets app-layer checks next (action E). Apply 0007 in Supabase. Remaining: office API checks (E), optional fitter row-scope on SELECT (F).
- **Role enforcement — UI gating (office + mobile)** — *v0.14.0*: screens now show only what the role allows. Office: `applyRole()`/`canCap()` hide the Dashboard/Teams/Monday-sync tabs and "+ New item"; item-row edit/sync controls fall back to read-only unless the role can edit/fit/sync; non-dashboard roles land on Items. Mobile: `src/lib/permissions.ts` mirrors the matrix, the app loads the user's role and hides "+ New job" (non-managers), hides survey "+ New" for fitters, scopes the item list to ready-to-fit for fitters, and shows the install-status editor only to fitters/office. RLS (0.13.0) stays the real boundary. Remaining: office API role checks (E, closes the service-role gap), optional fitter SELECT row-scope (F).
- **One plan per item (configurable)** — *v0.26.0*: default single-plan pinning; items pinned elsewhere show ‘on <plan>’ and aren’t re-placeable (unpin first). Office Plans toggle `pins_multi_plan` (tenants col, migration 0012) relaxes it for plan versions; enforced office + mobile + server (409 on cross-plan re-pin).
- **Mobile plan viewer** — *v0.25.0*: `PlanScreen` — Plan button on the items screen opens the job's plan (signed URL) with colour-coded pins; tap a pin → item; surveyors/office drop/move/unpin (tap item → tap plan), fitters/scanners read-only; plan selector + placed/unplaced filter. Completes the plan feature end-to-end.
- **Raise a snag on the phone** — *v0.24.0*: item detail → Raise a snag (surveyor/fitter/office) → description + photos → inserts a `kind='snag'` item (`-S<n>` code, install status Snag) mirroring the office logic; office schedules it, syncs to Monday. Online-only; scanners excluded.
- **Plan view with item pins — office** — *v0.23.0*: office Plans tab — upload a floor-plan/elevation per job (`job_plans` + `plans` storage bucket, migration 0011), pin items to (x,y) on the plan (`survey_items.plan_id/plan_x/plan_y`), colour-coded by install status, click-to-open, placed/unplaced filter, multiple plans per job. Manager-gated (upload=admin/office, pin=items.edit). Top Fieldwire borrow. Next: mobile plan viewer (tap pin → item; drop pin during survey).
- **Style picker size filters** — *v0.22.0*: imported the Clearview Window Types sheet (505 rows: Product Type/Wide/High/Opening/Fixed) into `src/lib/styleMeta.ts`; the Choose-style picker filters by Wide (1-6), High (1-3) and Type (Window/Door/Tilt&Turn) plus code search + pick ranking; tiles show wide×high + opening; picking auto-fills window type.
- **Full sketch catalogue (mobile)** — *v0.21.0*: all 391 real Clearview style sketches (from ~/sketches, keyed by design code) bundled into the app (`assets/styles/`, `src/lib/styleAssets.ts`) and shown as image tiles in the Choose-style picker; offline, searchable by code, ranked by `pick_events`. Picking sets `design_code`. Bundling chosen over Supabase Storage for offline field use; revisit Storage/`style_catalogue` if the catalogue outgrows the app bundle.
- **Visual style picker (mobile)** — *v0.20.0*: the concept model's "pick screen" — full-screen picker with SVG-sketched window/door layouts (`src/lib/windowStyles.ts`, `src/screens/StylePicker.tsx`), Window/Door/Tilt&turn + 1/2/3+ light filters, and a "MOST USED HERE" grid auto-ranked by pick frequency from `pick_events` (room-weighted). Sets `window_type` + `design_code`, records a pick_event, remembers last for "Same as last item". `design_code` → Monday "Design Code". Needs `react-native-svg`.
- **Fuller survey spec + scanner one-hands mode** — *v0.19.0*: mobile survey form now captures window type, safety glass, cill depth, transoms x1-3, mullions x1-3, open in/out, coupled, add-ons (lib `windowTypes.ts`; new `survey_items.window_type`, migration 0010, mapped to Monday 'Window Type'). Scanner gets an on-screen 'Add full details now' toggle to survey-while-scanning (saves surveyed) vs quick location-only scan. Config chosen: on-screen toggle (no admin/tenant setting).
- **Surveyor add-spec (mobile)** — *v0.18.0*: a surveyor opens a scanned item and taps **Add survey details** to reopen it in the full form (pre-filled location) and fill material/glazing/glass/sizes/team; **Save details** does a direct `survey_items` update (online) and sets stage `surveyed`. Completes the scanner\u2192surveyor two-pass flow. Also renamed the mobile item tag from the ambiguous "local" to "on Monday" / "saved \u00b7 not on Monday".
- **Scanner mode (mobile)** — *v0.17.0*: scanners get a streamlined **Scan item** form (location/identity only, stage `scanned`, optional photo) with **Save & scan next** (keeps location, bumps the item number) for fast first-pass capture; surveyors/office keep the full spec form. Two-pass field flow: scanner creates the skeleton, surveyor fills the spec. Next: surveyor edits an existing scanned item on the phone to add the spec.
- **Fitter team view + Monday team pull-back** — *v0.16.0*: a fitter login is assigned to one team (office Users tab; `app_users.team_id`, migration 0008) and the phone shows only that team's ready-to-fit items. Team→item assignment stays mastered in Monday: office Sync tab → **Pull fitters** reads the Monday Fitters column back into the app and sets `survey_items.team_id` by team-name match (one-way read-back, no re-sync loop; `monday.getColumnTextForItems` + `setItemTeamFromMonday`). Aligns with the model that Monday owns scheduling/commercial data and the field app reads it.
- **Role enforcement — office server (defence in depth)** — *v0.15.0*: every mutating office API endpoint now runs a `can(ctx.role, cap)` guard (create/edit/fit items, raise snags, sync/link Monday, manage teams, manage users). The office server uses the service-role key so it bypasses RLS — this closes that gap. Roles are now enforced in all three layers: DB RLS (0.13.0), office server (0.15.0), and both UIs (0.14.0). `office` may now manage teams and link/sync Monday boards (was admin-only), matching the matrix. Only optional item F (fitter SELECT row-scope in RLS) remains.
- **Mobile Sign in with Microsoft** — *v0.14.2*: Azure SSO on the phone login (shown when `EXPO_PUBLIC_AZURE_SSO_ENABLED=true`), so SSO-only field staff can sign in and get role-gated like everyone else. `src/lib/auth.ts` drives `supabase.auth.signInWithOAuth({provider:'azure'})` through an `expo-web-browser` auth session, returning via the `acefield://auth-callback` scheme (PKCE + implicit fallback). Azure unchanged (Supabase callback already registered for office SSO); only the mobile redirect is added to Supabase Redirect URLs. Install: `npx expo install expo-web-browser expo-auth-session expo-crypto`. Setup: docs/mobile-sso-setup.md.

## Later
- **Photo frame recognition** — *Phase A started (v0.11.0)*: recognition schema/taxonomy frozen in `@ace/shared`; server-side vision service `recognise.ts` (OpenAI-compatible, hosted or self-hosted Qwen-VL) + `POST /api/recognise/:itemId` + CLI `npm run recognise` returns a window's layout + style from its photo. Off until `VISION_API_URL/MODEL` are set. Next: wire the mobile app to call it and pre-fill the config picker; bootstrap the labelled dataset. Inference spec: `docs/window-segment-recognition.md`; data/model plan: `docs/frame-recognition-data-strategy.md`.
- **Monday → Supabase read-only pull** — only if field teams need to see office-owned data (PO status, dates, costs). Deliberately deferred; decision recorded: app owns field data one-way to Monday.
- **Axis Dynamics 365 integration** — bookings in, completions/SOR/payment out. Gated on Axis granting Dataverse API access. See the confidential strategy doc.
- **Supply-chain connectors** — push a surveyed item to Clearview (and others) to quote → order.
- **Commercialisation machinery** — billing (Stripe), self-serve onboarding, vendor admin, marketplace — when customer #2 is real.

## Decisions on record
- Supabase is the canonical store; Monday is a one-way output. Field data is created/edited only in the app.
- Field-level ownership: the app owns survey/install/snag fields; the office owns commercial fields in Monday.
- Monday column matching is by **title**, never hard-coded id (survives board duplication).
- Money stored as integer pennies. Secrets (service key, Monday token) are server-side only.
- A snag is a first-class item (own labour cost + team + lifecycle), not a child record — matching how Monday already models it as a peer item. The legacy `snags` table + `syncSnagsForItem` (duplicate-item approach) are superseded by this and remain only for the old CLI demos.
