# Copy to apps/mobile/.env. Only the PUBLIC/anon values — never the service-role key.
# Expo exposes EXPO_PUBLIC_* to the app at build time.
EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
